{
  "name": "attack-listener",
  "version": "1.0.0",
  "description": "Local server to receive exfiltrated data from vulnerable application",
  "main": "listener.js",
  "scripts": {
    "start": "node listener.js",
    "dev": "nodemon listener.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}